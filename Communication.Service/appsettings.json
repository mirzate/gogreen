{
  "RabbitMQConfig": {
    "HostName": "localhost",
    "Port": 5672,
    "UserName": "admin",
    "Password": "password",
    "DefaultQueue": "my_queue"
  },
  "Mailgun": {
    "apiKey": "${Mailgun__apiKey}",
    "domain": "${Mailgun__domain}"
  }
}
